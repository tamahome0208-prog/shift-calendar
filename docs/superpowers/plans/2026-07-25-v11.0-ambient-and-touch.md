# v11.0 生活に溶ける + 距離感軽量版 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** シフトカレンダーに Dynamic favicon / タブ countdown / 時間帯背景 / 💗ボタン(バイブ付) / 交換日記スレッド を追加し、"開いてない時も存在感、開いたら温もり" を実現する。

**Architecture:** すべて `build.py` の HTML/CSS/JS 内編集 + `firestore.rules` 追記。新 Firestore collection `signals` (ライトウェイト即時信号) + `notes/{YYYY-MM-DD}` (per-日スレッド)。既存 dateMemo は 触らず共存、交換日記は day-overlay 内の別セクションとして追加(spec の "移行" は複雑性回避のため keep-both に微調整)。5コミット分割。

**Tech Stack:** Python 3(build時) / HTML+CSS+JS(生成物)/ Firebase Firestore v10 / Web Vibration API / SVG data URI

**前提コマンド:**
- ビルド(xlsx 省略可能、v10.5 fix 済): `python "C:\Users\tamah\Desktop\shift_calendar\build.py"`
- 差分計測: `git diff --cached --stat`
- ロールバック: `git checkout build.py index.html manifest.json sw.js`
- Firestore rules デプロイ: `firebase deploy --only firestore:rules --project karenda-eba3e`

---

## File Structure

すべて `C:\Users\tamah\Desktop\shift_calendar\build.py` の編集 + `firestore.rules` 追記。

| セクション | 役割 | 行数目安 |
|---|---|---|
| JS: `updateFavicon/TabTitle/Ambient` | favicon/title 動的更新の共通関数 | +80 |
| JS: `applyTimeTheme()` + CSS transition | 時間帯背景切替 | +60 |
| JS: `identityStore` + 設定シート UI | 自分の person key 保持 | +40 |
| JS: `Storage.sendPulse/onPulseSnapshot` + spawnHearts + vibrate | 💗信号送信+受信+アニメ | +90 |
| HTML: 💗ボタン(hero-card 内) | 送信 UI | +15 |
| JS: `Storage.appendNote/markNoteRead/onNotesSnapshot` + thread UI + unread badges | 交換日記 | +130 |
| CSS: 各機能用 | 全機能横断 | +50 |
| `firestore.rules` | signals + notes ルール追加 | +8 |

**編集不可:** `firebase-config.js`, `events.json`, `extract_events.py`, `sw.js`(自動生成)

---

## Task 1: C1 — Dynamic favicon + タブ countdown + Ambient 骨組

**Phase:** C1(単一コミット)
**推定差分:** +90行

**Files:**
- Modify: `build.py` の JS(既存 `let firestoreShifts = {};` の近くに ambient system 追加)

### Step 1.1: FAV_TEMPLATES 定数を追加

- [ ] **Locate `let firestoreShifts = {};` を確認**

Run:
```bash
grep -n "^let firestoreShifts" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **その直下に以下を挿入**

```javascript
// v11.0 Ambient: Favicon SVG テンプレート
const FAV_TEMPLATES = {
  normal:  '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><circle cx="16" cy="16" r="14" fill="#84CC16"/><circle cx="11" cy="12" r="2.5" fill="white"/><circle cx="21" cy="12" r="2.5" fill="white"/><circle cx="11" cy="12" r="1.2" fill="black"/><circle cx="21" cy="12" r="1.2" fill="black"/><path d="M 10 20 Q 16 24 22 20" stroke="black" stroke-width="1.5" fill="none" stroke-linecap="round"/></svg>',
  bothoff: '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><path d="M16 28 C 6 18 6 10 12 10 Q 16 10 16 14 Q 16 10 20 10 C 26 10 26 18 16 28 Z" fill="#F764A2" stroke="#B91C5C" stroke-width="1.5"/></svg>',
  salary:  '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><circle cx="16" cy="16" r="12" fill="#FCD34D" stroke="#B45309" stroke-width="1.5"/><text x="16" y="21" text-anchor="middle" font-size="14" font-weight="900" fill="#78350F" font-family="sans-serif">¥</text></svg>',
  birth:   '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect x="6" y="16" width="20" height="10" rx="1.5" fill="#FFD9E6" stroke="#F764A2" stroke-width="1"/><rect x="14" y="8" width="3" height="8" fill="#FBBF24" rx="0.6"/><path d="M15.5 5 L16.5 8 L14.5 8 Z" fill="#FF8E5C"/></svg>',
  anniv:   '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><path d="M16 28 C 6 18 6 10 12 10 Q 16 10 16 14 Q 16 10 20 10 C 26 10 26 18 16 28 Z" fill="#F5C6E8" stroke="#B91C5C" stroke-width="1.5"/></svg>',
  other:   '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><path d="M16 4 L18 12 L26 12 L20 17 L22 25 L16 20 L10 25 L12 17 L6 12 L14 12 Z" fill="#B593D6" stroke="#7C3DA0" stroke-width="1"/></svg>',
};
```

### Step 1.2: updateFavicon() を追加

- [ ] **FAV_TEMPLATES の直下に追加**

```javascript
function updateFavicon() {
  const todayKey = dateKey(new Date().getFullYear(), new Date().getMonth(), new Date().getDate());
  let state = 'normal';
  // 優先順: 記念日 > 給料日 > 両休み > 通常
  const [_y, _m, _d] = todayKey.split('-').map(Number);
  const anniv = (allAnniversaries || []).find(a => a.month === _m && a.day === _d);
  if (anniv) {
    const t = anniv.type || 'other';
    state = t === 'birth' ? 'birth' : (t === 'anniv' ? 'anniv' : 'other');
  } else {
    const evs = getMergedEvents()[todayKey] || [];
    if (evs.some(e => e.person === '給料')) state = 'salary';
    else if (isBothOff(EVENTS[todayKey] || [])) state = 'bothoff';
  }
  const svg = FAV_TEMPLATES[state] || FAV_TEMPLATES.normal;
  const uri = 'data:image/svg+xml;utf8,' + encodeURIComponent(svg);
  let link = document.querySelector('link[rel="icon"]');
  if (!link) {
    link = document.createElement('link');
    link.rel = 'icon';
    document.head.appendChild(link);
  }
  link.href = uri;
}
```

### Step 1.3: updateTabTitle() を追加

- [ ] **updateFavicon の直下に追加**

```javascript
function updateTabTitle() {
  const today = new Date();
  const todayKey = dateKey(today.getFullYear(), today.getMonth(), today.getDate());
  const [_ty, _tm, _td] = todayKey.split('-').map(Number);

  // 1. 今日が特別
  const anniv = (allAnniversaries || []).find(a => a.month === _tm && a.day === _td);
  if (anniv) {
    const emoji = anniv.type === 'birth' ? '🎂' : (anniv.type === 'anniv' ? '💗' : '🌸');
    document.title = `${emoji} 今日は${anniv.name} ・カレンダー`;
    return;
  }
  const evs = getMergedEvents()[todayKey] || [];
  if (evs.some(e => e.person === '給料')) {
    document.title = '💰 今日は給料日 ・カレンダー';
    return;
  }
  if (isBothOff(EVENTS[todayKey] || [])) {
    document.title = '💗 今日はふたりお休み ・カレンダー';
    return;
  }

  // 2. 7日以内の最近接
  const candidates = [];
  for (let i = 1; i <= 7; i++) {
    const d = new Date(today.getFullYear(), today.getMonth(), today.getDate() + i);
    const key = dateKey(d.getFullYear(), d.getMonth(), d.getDate());
    const [__y, __m, __d] = key.split('-').map(Number);
    const a = (allAnniversaries || []).find(x => x.month === __m && x.day === __d);
    if (a) { candidates.push({days: i, label: a.name}); continue; }
    const ev = getMergedEvents()[key] || [];
    if (ev.some(e => e.person === '給料')) { candidates.push({days: i, label: '給料日'}); continue; }
    if (isBothOff(EVENTS[key] || [])) { candidates.push({days: i, label: 'ふたりお休み'}); continue; }
  }
  if (candidates.length) {
    const c = candidates[0];
    const prefix = c.days === 1 ? '明日は' : `あと${c.days}日で`;
    document.title = `${prefix}${c.label} ・カレンダー`;
    return;
  }

  // 3. 通常
  document.title = `カレンダー(${today.getFullYear()}年${today.getMonth()+1}月)`;
}
```

### Step 1.4: updateAmbient() 統合 + init/interval 配線

- [ ] **updateTabTitle の直下に追加**

```javascript
let __ambientTimer = null;
function updateAmbient() {
  if (document.visibilityState === 'hidden') return;
  updateFavicon();
  updateTabTitle();
  // applyTimeTheme() は C2 で追加
}
function startAmbient() {
  updateAmbient();
  if (__ambientTimer) clearInterval(__ambientTimer);
  __ambientTimer = setInterval(updateAmbient, 15 * 60 * 1000);
  document.addEventListener('visibilitychange', updateAmbient);
}
```

- [ ] **`init()` の末尾に呼び出しを追加**

`grep -n "await Storage.init" "C:\Users\tamah\Desktop\shift_calendar\build.py"` で位置確認。`await Storage.init();` の直後、`flushShiftQueue().catch(...)` の後に追加:

```javascript
  // v11.0 Ambient system 起動
  startAmbient();
```

### Step 1.5: 月切替時にも ambient 更新

- [ ] **Locate `function render()`**

Run:
```bash
grep -n "^function render()" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **render() の末尾(閉じ `}` の直前)に追加**

`render()` の閉じ `}` を Read で見つけて、その直前に:
```javascript
  // v11.0 月切替や再描画のたびに tab title を最新化(favicon は今日基準なので不要)
  if (typeof updateTabTitle === 'function') updateTabTitle();
```

### Step 1.6: ビルド + 検証 + コミット

- [ ] **ビルド**

```bash
python "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

期待: `index.html (151件のイベント)`

- [ ] **preview で検証**

Preview 起動後、DevTools Console で:
```javascript
document.title  // 動的タイトルが表示されているか
document.querySelector('link[rel="icon"]').href.startsWith('data:image/svg+xml')  // true
typeof updateAmbient  // "function"
```

- [ ] **差分計測**

```bash
cd "C:\Users\tamah\Desktop\shift_calendar"
git add -A
git diff --cached --stat
```

Expected: build.py +80〜110行、150以内。

- [ ] **Commit + push**

```bash
git commit -m "$(cat <<'EOF'
v11.0-C1: Dynamic favicon + タブ countdown + ambient 骨組

- FAV_TEMPLATES に 6状態のSVG(通常/両休み/給料日/誕生日/記念日/その他)
- updateFavicon: 優先順(記念日>給料日>両休み>通常)で favicon 動的差替
- updateTabTitle: 今日=特別/7日以内=近未来/その他=通常 の3パターン
- updateAmbient: 15分毎+visibilitychange+init時に一括更新
- render() 内でも tab title 再計算(月切替反映)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 2: C2 — 時間帯背景

**Phase:** C2(単一コミット)
**推定差分:** +70行

**Files:**
- Modify: `build.py` CSS(`</style>` 直前に transition + reduce-motion 対応)
- Modify: `build.py` JS(applyTimeTheme + updateAmbient に統合)

### Step 2.1: CSS 追加

- [ ] **Locate `</style>` 終端**

Run:
```bash
grep -n "^</style>" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **`</style>` の直前に挿入**

```css
/* v11.0 時間帯背景 */
body {
  transition: background 0.8s ease;
}
body.time-morning {
  background: linear-gradient(180deg, #FFE4B5 0%, #FFF8F0 100%);
}
body.time-noon {
  background: linear-gradient(180deg, #E0F2FE 0%, #FFF8F0 100%);
}
body.time-evening {
  background: linear-gradient(180deg, #FDBA74 0%, #FFE4E1 100%);
}
body.time-night {
  background: linear-gradient(180deg, #1E3A8A 0%, #4B3F72 100%);
}
@media (prefers-reduced-motion: reduce) {
  body { transition: none; }
}
```

### Step 2.2: applyTimeTheme() を追加

- [ ] **Locate `function updateAmbient()`**

Run:
```bash
grep -n "^function updateAmbient" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **updateAmbient の**直前**に applyTimeTheme を挿入**

```javascript
function applyTimeTheme() {
  const h = new Date().getHours();
  let mode;
  if (h >= 5 && h < 10) mode = 'morning';
  else if (h >= 10 && h < 16) mode = 'noon';
  else if (h >= 16 && h < 19) mode = 'evening';
  else mode = 'night';
  const classes = ['time-morning', 'time-noon', 'time-evening', 'time-night'];
  document.body.classList.remove(...classes);
  document.body.classList.add(`time-${mode}`);
}
```

### Step 2.3: updateAmbient に統合

- [ ] **updateAmbient を以下に置換(applyTimeTheme 呼び出し追加)**

既存(C1 で追加):
```javascript
function updateAmbient() {
  if (document.visibilityState === 'hidden') return;
  updateFavicon();
  updateTabTitle();
  // applyTimeTheme() は C2 で追加
}
```

変更後:
```javascript
function updateAmbient() {
  if (document.visibilityState === 'hidden') return;
  updateFavicon();
  updateTabTitle();
  applyTimeTheme();
}
```

### Step 2.4: 初回起動時は transition なしで即適用

- [ ] **`startAmbient()` を以下に置換**

既存(C1で追加):
```javascript
function startAmbient() {
  updateAmbient();
  if (__ambientTimer) clearInterval(__ambientTimer);
  __ambientTimer = setInterval(updateAmbient, 15 * 60 * 1000);
  document.addEventListener('visibilitychange', updateAmbient);
}
```

変更後(初回のみ transition 無効化):
```javascript
function startAmbient() {
  // 初回は transition なしで即適用
  document.body.style.transition = 'none';
  updateAmbient();
  // 次フレームで transition を戻す
  requestAnimationFrame(() => {
    document.body.style.transition = '';
  });
  if (__ambientTimer) clearInterval(__ambientTimer);
  __ambientTimer = setInterval(updateAmbient, 15 * 60 * 1000);
  document.addEventListener('visibilitychange', updateAmbient);
}
```

### Step 2.5: ビルド + 検証 + コミット

- [ ] **ビルド**

```bash
python "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **preview で検証**

DevTools Console:
```javascript
document.body.classList.value  // "time-morning" or similar
typeof applyTimeTheme  // "function"

// 手動で夜モード試験
document.body.classList.remove('time-morning','time-noon','time-evening','time-night');
document.body.classList.add('time-night');
// 濃紺グラデが 0.8s ease で見えるか
```

- [ ] **差分 + commit + push**

```bash
git add -A && git diff --cached --stat
git commit -m "$(cat <<'EOF'
v11.0-C2: 時間帯背景(朝/昼/夕/夜)

- body.time-{morning,noon,evening,night} クラスで gradient 切替
- applyTimeTheme: 現時刻から mode 判定して class 付与
- updateAmbient に統合、15分毎+visibilitychange で自動更新
- 初回起動時は transition なしで即適用、以降は 0.8s ease
- prefers-reduced-motion で transition 無効化

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 3: C3 — Identity セレクタ + Signals 基盤

**Phase:** C3(単一コミット)
**推定差分:** +100行

**Files:**
- Modify: `build.py` 設定シート HTML(identity セレクタ追加)
- Modify: `build.py` JS(identityStore + Storage.sendPulse/onPulseSnapshot)
- Modify: `firestore.rules`(signals 追記)
- 実行: `firebase deploy --only firestore:rules`

### Step 3.1: identityStore を追加

- [ ] **Locate `let firestoreShifts = {};`**

Run:
```bash
grep -n "^let firestoreShifts" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **その近く(FAV_TEMPLATES より前)に以下を挿入**

```javascript
// v11.0 identityStore: 自分が こうき / ゆい どちらかを保持
const identityStore = {
  KEY: 'sc_my_person',
  get() {
    return localStorage.getItem(this.KEY);
  },
  set(v) {
    if (v === 'kouki' || v === 'yui') {
      localStorage.setItem(this.KEY, v);
    } else {
      localStorage.removeItem(this.KEY);
    }
  },
};
let firestorePulse = null;
let unsubscribePulse = null;
let __lastPulseTs = 0;
```

### Step 3.2: Storage.sendPulse / onPulseSnapshot 追加

- [ ] **Locate `Storage.saveShifts` メソッド**

Run:
```bash
grep -n "^  async saveShifts" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **saveShifts の直下(閉じ `,` の後)に以下を挿入**

```javascript
  async sendPulse(kind = 'heart') {
    if (!firestoreDb) return;
    const me = identityStore.get();
    if (!me) return;
    await this._setDoc(this._doc(firestoreDb, 'signals', 'pulse'), {
      from: me,
      ts: new Date(),
      kind,
    });
  },
```

### Step 3.3: initFirebase で signals/pulse を購読

- [ ] **Locate `this._collShifts = collection(firestoreDb, 'shifts');`**

Run:
```bash
grep -n "_collShifts = collection" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **既存の shifts onSnapshot ブロックの直下(次の `});` の後)に以下を追加**

```javascript
    // v11.0 signals/pulse purchase
    if (unsubscribePulse) unsubscribePulse();
    unsubscribePulse = onSnapshot(this._doc(firestoreDb, 'signals', 'pulse'), snap => {
      const data = snap.data();
      if (!data) return;
      const me = identityStore.get();
      const ts = data.ts?.toMillis ? data.ts.toMillis() : 0;
      if (!me || data.from === me || ts <= __lastPulseTs) return;
      __lastPulseTs = ts;
      // C4 で receivePulse() 実装(現段階では確認ログのみ)
      if (typeof receivePulse === 'function') receivePulse(data);
      else console.log('[Pulse] received (no receiver yet):', data);
    });
```

### Step 3.4: 設定シートに identity セレクタ追加

- [ ] **Locate `class="field"` の「シフトPDFをアップロード」直前(または好みの位置)**

Run:
```bash
grep -n 'シフトPDFをアップロード' "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **「シフトPDFをアップロード」フィールドの**直前**に以下を挿入**

既存 HTML:
```html
      <div class="field">
        <label style="display:flex;align-items:center;gap:6px;"><svg style="width:16px;height:16px;"><use href="#i-cloud"/></svg> シフトPDFをアップロード</label>
```

変更後(直前に新規 field を挿入):
```html
      <div class="field">
        <label style="display:flex;align-items:center;gap:6px;">あなたは誰?</label>
        <div style="font-size: 12px; color: var(--muted); margin-bottom: 6px;">💗ボタンや交換日記の送信元判定に使います</div>
        <div style="display:flex;gap:8px;">
          <button class="btn btn-secondary" id="identity-kouki" type="button" data-key="kouki" style="flex:1;padding:12px;">こうき</button>
          <button class="btn btn-secondary" id="identity-yui" type="button" data-key="yui" style="flex:1;padding:12px;">ゆい</button>
        </div>
        <div id="identity-status" style="font-size: 12px; color: var(--muted); margin-top: 6px;"></div>
      </div>
      <div class="field">
        <label style="display:flex;align-items:center;gap:6px;"><svg style="width:16px;height:16px;"><use href="#i-cloud"/></svg> シフトPDFをアップロード</label>
```

### Step 3.5: identity セレクタのハンドラ配線

- [ ] **Locate `document.getElementById('settings-close').onclick`**

Run:
```bash
grep -n "settings-close.*onclick" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **既存の settings 系ハンドラ集約箇所に追加**

```javascript
  function refreshIdentityUI() {
    const me = identityStore.get();
    const kBtn = document.getElementById('identity-kouki');
    const yBtn = document.getElementById('identity-yui');
    const status = document.getElementById('identity-status');
    if (kBtn) kBtn.classList.toggle('btn-primary', me === 'kouki');
    if (kBtn) kBtn.classList.toggle('btn-secondary', me !== 'kouki');
    if (yBtn) yBtn.classList.toggle('btn-primary', me === 'yui');
    if (yBtn) yBtn.classList.toggle('btn-secondary', me !== 'yui');
    if (status) {
      status.textContent = me ? `現在: ${me === 'kouki' ? 'こうき' : 'ゆい'}` : '未設定';
      status.style.color = me ? 'var(--leaf-deep)' : 'var(--muted)';
    }
  }
  const kBtn = document.getElementById('identity-kouki');
  const yBtn = document.getElementById('identity-yui');
  if (kBtn) kBtn.onclick = () => { identityStore.set('kouki'); refreshIdentityUI(); };
  if (yBtn) yBtn.onclick = () => { identityStore.set('yui'); refreshIdentityUI(); };
  refreshIdentityUI();
```

### Step 3.6: firestore.rules に追記

- [ ] **`firestore.rules` を Read で確認**

Run:
```bash
grep -n "match /shifts" "C:\Users\tamah\Desktop\shift_calendar\firestore.rules"
```

- [ ] **既存の shifts ルールブロックの直下(閉じ `}` の後)に追加**

既存:
```
    match /shifts/{ym} {
      allow read, write: if request.auth != null;
    }

  }
}
```

変更後:
```
    match /shifts/{ym} {
      allow read, write: if request.auth != null;
    }

    // v11.0 signals/pulse - リアルタイム軽量信号
    match /signals/{docId} {
      allow read, write: if request.auth != null;
    }

  }
}
```

### Step 3.7: Firestore rules デプロイ

- [ ] **Run**

```bash
cd "C:\Users\tamah\Desktop\shift_calendar"
firebase deploy --only firestore:rules --project karenda-eba3e
```

Expected: `+  Deploy complete!`

### Step 3.8: ビルド + 検証 + コミット

- [ ] **ビルド**

```bash
python "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **preview で検証**

DevTools Console:
```javascript
typeof identityStore  // "object"
identityStore.set('kouki'); identityStore.get()  // "kouki"

// 設定シート開いて識別ボタンを試す
document.getElementById('identity-kouki').click();
document.getElementById('identity-status').textContent  // "現在: こうき"
```

- [ ] **差分 + commit + push**

```bash
git add -A && git diff --cached --stat
git commit -m "$(cat <<'EOF'
v11.0-C3: Identity セレクタ + Signals collection 基盤

- identityStore: sc_my_person (localStorage) で kouki/yui 保持
- 設定シートに「あなたは誰?」ボタン2択(こうき/ゆい)追加
- Storage.sendPulse: signals/pulse に setDoc
- initFirebase で signals/pulse onSnapshot 購読 → 受信ロジックは C4 で実装
- firestore.rules に /signals/{docId} 追記 + deploy 済

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 4: C4 — 💗ボタン + spawnHearts + バイブ

**Phase:** C4(単一コミット)
**推定差分:** +100行

**Files:**
- Modify: `build.py` hero-card HTML(💗ボタン追加)
- Modify: `build.py` CSS(💗ボタン + heart-fly-up)
- Modify: `build.py` JS(spawnHearts + receivePulse + ボタンハンドラ + 5秒 disabled)

### Step 4.1: CSS 追加

- [ ] **`</style>` 直前に挿入**

```css
/* v11.0 💗ボタン + spawnHearts */
.pulse-btn {
  position: absolute;
  top: 12px;
  right: 12px;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: none;
  background: linear-gradient(135deg, var(--cheek) 0%, var(--cheek-deep) 100%);
  color: white;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2px 6px rgba(247, 100, 162, 0.4);
  transition: transform 0.15s var(--ease-spring);
  z-index: 5;
}
.pulse-btn:active { transform: scale(0.88); }
.pulse-btn:disabled { opacity: 0.5; cursor: wait; }
.pulse-btn svg { width: 18px; height: 18px; }
.pulse-btn-countdown {
  position: absolute;
  bottom: -14px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 10px;
  font-weight: 800;
  color: var(--muted);
  pointer-events: none;
}
.pulse-heart {
  position: fixed;
  bottom: 0;
  font-size: 32px;
  pointer-events: none;
  z-index: 9998;
  animation: heart-fly-up 1.6s cubic-bezier(0.4, 0.2, 0.3, 1) forwards;
}
@keyframes heart-fly-up {
  from { opacity: 0; transform: translateY(0) scale(0.6); }
  30%  { opacity: 1; }
  to   { opacity: 0; transform: translateY(-320px) scale(1.2) rotate(15deg); }
}
@media (prefers-reduced-motion: reduce) {
  .pulse-heart { animation: none; opacity: 0; }
  .pulse-btn { transition: none; }
}
```

### Step 4.2: hero-card HTML に💗ボタン追加

- [ ] **Locate `<section class="hero-card"`**

Run:
```bash
grep -n '<section class="hero-card"' "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **`<section class="hero-card" id="hero-card">` の直後の行に追加**

既存:
```html
<section class="hero-card" id="hero-card">
  <div class="hero-status" id="hero-status">…</div>
```

変更後(hero-status の直前に💗ボタンを挿入):
```html
<section class="hero-card" id="hero-card">
  <button class="pulse-btn" id="pulse-btn" type="button" aria-label="💗を送る" title="💗を送る">
    <svg viewBox="0 0 24 24"><use href="#heart"/></svg>
    <span class="pulse-btn-countdown" id="pulse-btn-countdown"></span>
  </button>
  <div class="hero-status" id="hero-status">…</div>
```

### Step 4.3: spawnHearts + receivePulse を追加

- [ ] **Locate `let __lastPulseTs = 0;`(C3 で追加済)**

Run:
```bash
grep -n "__lastPulseTs" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **その定義の直下に以下を追加**

```javascript
function spawnHearts(count = 8) {
  if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
  const container = document.body;
  for (let i = 0; i < count; i++) {
    const el = document.createElement('div');
    el.className = 'pulse-heart';
    el.textContent = '💗';
    el.style.left = `${Math.random() * 80 + 10}%`;
    el.style.animationDelay = `${Math.random() * 0.4}s`;
    el.style.fontSize = `${24 + Math.random() * 20}px`;
    container.appendChild(el);
    setTimeout(() => el.remove(), 2500);
  }
}

function receivePulse(data) {
  spawnHearts(8);
  if (navigator.vibrate) {
    navigator.vibrate([80, 40, 80]);
  }
}

let __pulseBtnCooldown = 0;
async function handlePulseClick() {
  const me = identityStore.get();
  if (!me) {
    alert('まず設定で「あなたは誰?」を選んでください');
    return;
  }
  if (Date.now() < __pulseBtnCooldown) return;
  __pulseBtnCooldown = Date.now() + 5000;
  spawnHearts(8);
  const btn = document.getElementById('pulse-btn');
  const cd = document.getElementById('pulse-btn-countdown');
  if (btn) btn.disabled = true;
  let secs = 5;
  if (cd) cd.textContent = `${secs}s`;
  const iv = setInterval(() => {
    secs -= 1;
    if (cd) cd.textContent = secs > 0 ? `${secs}s` : '';
    if (secs <= 0) {
      clearInterval(iv);
      if (btn) btn.disabled = false;
    }
  }, 1000);
  try {
    await Storage.sendPulse('heart');
  } catch (e) {
    console.error('[Pulse] send failed:', e);
  }
}
```

### Step 4.4: 💗ボタン handler 配線

- [ ] **Locate `settings-close.*onclick` 近辺(既存 handler 集約箇所)**

Run:
```bash
grep -n "settings-close.*onclick" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **その周辺に追加**

```javascript
  const pulseBtn = document.getElementById('pulse-btn');
  if (pulseBtn) pulseBtn.onclick = handlePulseClick;
```

### Step 4.5: ビルド + 検証 + コミット

- [ ] **ビルド**

```bash
python "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **preview で検証**

```javascript
// identity 未設定なら alert
document.getElementById('pulse-btn').click();

// identity 設定してからテスト
identityStore.set('kouki');
document.getElementById('pulse-btn').click();
// → 画面に💗浮かぶ、5秒間 disabled
document.querySelectorAll('.pulse-heart').length  // 8

// receivePulse を手動発火
receivePulse({from: 'yui', kind: 'heart'});
// → 💗浮かぶ + バイブ(端末対応時)
```

- [ ] **差分 + commit + push**

```bash
git add -A && git diff --cached --stat
git commit -m "$(cat <<'EOF'
v11.0-C4: 💗ボタン + spawnHearts + バイブ

- hero-card 右上に💗ボタン(丸36px、cheek gradient)
- タップで 自分側に💗8個ふわり(1.6s) + Storage.sendPulse
- 5秒 disabled(連打防止、カウントダウン数字表示)
- 相手からの pulse 受信で spawnHearts + navigator.vibrate
- identity 未設定なら alert 表示
- prefers-reduced-motion で全アニメ無効化

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 5: C5 — 交換日記 (notes) + 未読バッジ + rules

**Phase:** C5(単一コミット)
**推定差分:** +180行

**Files:**
- Modify: `build.py` JS(notes state + Storage methods + thread UI + unread badges)
- Modify: `build.py` CSS(スレッド UI + 未読バッジ)
- Modify: `firestore.rules`(notes 追記)
- 実行: `firebase deploy --only firestore:rules`

### Step 5.1: notes state + Storage methods を追加

- [ ] **Locate `let __lastPulseTs = 0;`**

- [ ] **その直下に以下を追加**

```javascript
let firestoreNotes = {};  // { "2026-08-15": { messages: [...], updatedAt } }
let unsubscribeNotes = null;
```

- [ ] **Locate `async sendPulse` メソッド(C3 で追加済)**

Run:
```bash
grep -n "^  async sendPulse" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **sendPulse の直下に追加**

```javascript
  async appendNote(dateKey, text) {
    if (!firestoreDb) return;
    const me = identityStore.get();
    if (!me || !text.trim()) return;
    const trimmed = text.trim().slice(0, 200);
    const msg = {
      id: `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
      from: me,
      text: trimmed,
      ts: Date.now(),
      readBy: [me],
    };
    const ref = this._doc(firestoreDb, 'notes', dateKey);
    const snap = await this._getDoc(ref);
    const existing = snap.exists() ? (snap.data().messages || []) : [];
    await this._setDoc(ref, {
      messages: [...existing, msg],
      updatedAt: new Date(),
    });
  },
  async markNoteRead(dateKey, msgIds) {
    if (!firestoreDb) return;
    const me = identityStore.get();
    if (!me || !msgIds.length) return;
    const ref = this._doc(firestoreDb, 'notes', dateKey);
    const snap = await this._getDoc(ref);
    if (!snap.exists()) return;
    const messages = (snap.data().messages || []).map(m => {
      if (msgIds.includes(m.id) && !(m.readBy || []).includes(me)) {
        return { ...m, readBy: [...(m.readBy || []), me] };
      }
      return m;
    });
    await this._setDoc(ref, { messages, updatedAt: new Date() });
  },
```

### Step 5.2: initFirebase で getDoc + notes onSnapshot 購読

- [ ] **Locate 既存の `import` line for firestore SDK**

Run:
```bash
grep -n "import\(.*firebase-firestore" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **既存の import 行に `getDoc` を追加**

既存:
```javascript
    const { getFirestore, collection, onSnapshot, addDoc, deleteDoc, doc, setDoc, query, orderBy } = await import('https://www.gstatic.com/firebasejs/10.13.0/firebase-firestore.js');
```

変更後:
```javascript
    const { getFirestore, collection, onSnapshot, addDoc, deleteDoc, doc, setDoc, query, orderBy, getDoc, where } = await import('https://www.gstatic.com/firebasejs/10.13.0/firebase-firestore.js');
```

- [ ] **既存の Storage 初期化行 `this._addDoc = addDoc; ...` に `_getDoc` と `_where` `_query` を追加**

Run:
```bash
grep -n "this._addDoc = addDoc" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

既存:
```javascript
    this._addDoc = addDoc; this._deleteDoc = deleteDoc; this._doc = doc; this._setDoc = setDoc;
```

変更後:
```javascript
    this._addDoc = addDoc; this._deleteDoc = deleteDoc; this._doc = doc; this._setDoc = setDoc; this._getDoc = getDoc; this._where = where; this._query = query;
```

- [ ] **Locate 既存の signals/pulse 購読ブロック(C3 で追加)**

Run:
```bash
grep -n "signals/pulse purchase" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **signals 購読ブロックの直下に notes 購読を追加**

```javascript
    // v11.0 notes purchase(現月+前後1ヶ月)
    this._collNotes = collection(firestoreDb, 'notes');
    if (unsubscribeNotes) unsubscribeNotes();
    const rebuildNotesListener = () => {
      if (unsubscribeNotes) unsubscribeNotes();
      const y = viewYear;
      const m = viewMonth;
      const start = new Date(y, m - 1, 1);
      const end = new Date(y, m + 2, 0);
      const startKey = dateKey(start.getFullYear(), start.getMonth(), start.getDate());
      const endKey = dateKey(end.getFullYear(), end.getMonth(), end.getDate());
      const q = this._query(this._collNotes, this._where('__name__', '>=', startKey), this._where('__name__', '<=', endKey));
      unsubscribeNotes = onSnapshot(q, snap => {
        const next = {};
        snap.forEach(doc => { next[doc.id] = doc.data(); });
        firestoreNotes = next;
        render();
      });
    };
    rebuildNotesListener();
    window.__rebuildNotesListener = rebuildNotesListener;
```

### Step 5.3: 月切替時に notes listener 再構築

- [ ] **Locate `function shift(delta)`**

Run:
```bash
grep -n "^function shift(delta)" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **shift() 内、`render()` 呼び出しの後(既存のスライドロジックの中)に追加**

reduced-motion 分岐:
```javascript
  if (!topRow || reduced) {
    render();
    saveLastView();
    if (window.__rebuildNotesListener) window.__rebuildNotesListener();
    return;
  }
```

setTimeout 分岐(既存の内側 render の後):
```javascript
  pendingShiftOut = setTimeout(() => {
    pendingShiftOut = null;
    render();
    saveLastView();
    if (window.__rebuildNotesListener) window.__rebuildNotesListener();
    topRow.classList.remove(outCls);
    // ... 既存の続き
```

### Step 5.4: CSS 追加(スレッド + 未読バッジ)

- [ ] **`</style>` 直前に挿入**

```css
/* v11.0 交換日記スレッド */
.note-thread {
  display: flex;
  flex-direction: column;
  gap: 6px;
  max-height: 40vh;
  overflow-y: auto;
  padding: 4px;
  margin-bottom: 10px;
}
.note-msg {
  padding: 8px 12px;
  border-radius: 14px;
  font-size: 13px;
  max-width: 78%;
  word-break: break-word;
  line-height: 1.4;
  position: relative;
}
.note-msg.me {
  align-self: flex-end;
  background: linear-gradient(135deg, var(--kouki-meeting) 0%, #1B5577 100%);
  color: white;
  border-bottom-right-radius: 4px;
}
.note-msg.them {
  align-self: flex-start;
  background: var(--surface-soft);
  color: var(--text);
  border: 1px solid var(--border);
  border-bottom-left-radius: 4px;
}
.note-msg.them.unread {
  border-color: var(--cheek-deep);
  background: rgba(247, 100, 162, 0.05);
}
.note-meta {
  font-size: 10px;
  opacity: 0.72;
  margin-top: 2px;
  display: flex;
  gap: 6px;
}
.note-input-row {
  display: flex;
  gap: 6px;
  padding: 6px 4px;
  border-top: 1px solid var(--border);
}
.note-input-row input {
  flex: 1;
  padding: 8px 10px;
  border-radius: 10px;
  border: 1.5px solid var(--border);
  font-size: 13px;
}
.note-input-row button {
  padding: 8px 14px;
  border-radius: 10px;
  border: none;
  background: var(--cheek-deep);
  color: white;
  font-weight: 800;
  cursor: pointer;
}
.note-empty {
  text-align: center;
  color: var(--muted);
  font-size: 12px;
  padding: 12px 0;
}
.unread-badge {
  position: absolute;
  top: 2px;
  right: 2px;
  min-width: 14px;
  height: 14px;
  padding: 0 3px;
  border-radius: 7px;
  background: var(--cheek-deep);
  color: white;
  font-size: 9px;
  font-weight: 900;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2;
  pointer-events: none;
}
```

### Step 5.5: renderNoteThread + computeUnreadForDay 追加

- [ ] **Locate `function buildDateMemoCard`**

Run:
```bash
grep -n "^function buildDateMemoCard" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **buildDateMemoCard の**直前**に以下を追加**

```javascript
function renderNoteThread(dateKey) {
  const wrap = document.createElement('div');
  wrap.className = 'note-thread-wrap';
  const me = identityStore.get();
  const doc = firestoreNotes[dateKey] || { messages: [] };
  const messages = doc.messages || [];

  const title = document.createElement('div');
  title.style.cssText = 'font-weight:800;font-size:13px;color:var(--text-2);margin-bottom:6px;';
  title.textContent = '💬 交換日記';
  wrap.appendChild(title);

  const thread = document.createElement('div');
  thread.className = 'note-thread';
  if (!messages.length) {
    const empty = document.createElement('div');
    empty.className = 'note-empty';
    empty.textContent = 'まだメッセージはありません';
    thread.appendChild(empty);
  } else {
    const unreadIds = [];
    messages.forEach(m => {
      const isMe = m.from === me;
      const isUnread = !isMe && !(m.readBy || []).includes(me);
      if (isUnread) unreadIds.push(m.id);
      const card = document.createElement('div');
      card.className = `note-msg ${isMe ? 'me' : 'them'}${isUnread ? ' unread' : ''}`;
      const sender = m.from === 'kouki' ? 'こうき' : 'ゆい';
      const time = new Date(m.ts).toLocaleTimeString('ja-JP', { hour: '2-digit', minute: '2-digit' });
      card.innerHTML = `<div>${escapeHtml(m.text)}</div><div class="note-meta">${sender} · ${time}${isMe && (m.readBy || []).length > 1 ? ' · ✓既読' : ''}</div>`;
      thread.appendChild(card);
    });
    if (unreadIds.length && me) {
      Storage.markNoteRead(dateKey, unreadIds).catch(e => console.error('[note] markRead failed:', e));
    }
  }
  wrap.appendChild(thread);
  requestAnimationFrame(() => { thread.scrollTop = thread.scrollHeight; });

  const inputRow = document.createElement('div');
  inputRow.className = 'note-input-row';
  inputRow.innerHTML = `<input type="text" placeholder="${me ? 'メッセージを書く…' : 'まず設定で自分を選んでね'}" maxlength="200"${me ? '' : ' disabled'}><button type="button"${me ? '' : ' disabled'}>送信</button>`;
  const input = inputRow.querySelector('input');
  const send = inputRow.querySelector('button');
  const doSend = async () => {
    const txt = input.value.trim();
    if (!txt) return;
    input.value = '';
    send.disabled = true;
    try {
      await Storage.appendNote(dateKey, txt);
    } catch (e) {
      console.error('[note] send failed:', e);
      alert('送信失敗: ' + (e.message || e));
    } finally {
      send.disabled = !me;
    }
  };
  send.onclick = doSend;
  input.onkeydown = e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); doSend(); } };
  wrap.appendChild(inputRow);

  return wrap;
}

function computeUnreadForDay(key) {
  const me = identityStore.get();
  if (!me) return 0;
  const doc = firestoreNotes[key];
  if (!doc || !doc.messages) return 0;
  return doc.messages.filter(m => m.from !== me && !(m.readBy || []).includes(me)).length;
}
```

### Step 5.6: refreshDaySheet に note thread を追加

- [ ] **Locate `function refreshDaySheet`**

Run:
```bash
grep -n "^function refreshDaySheet" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **既存の buildDateMemoCard 追加箇所の**直後**に note thread を追加**

Run:
```bash
grep -n "buildDateMemoCard(key)" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **isBothOff 分岐内の `body.appendChild(buildDateMemoCard(key));` の直後**、および `if (evs.length === 0 && annivsForDay.length === 0)` ブロックの前(常に交換日記は出したい)に追加

現状の refreshDaySheet で isBothOff だけで buildDateMemoCard 出してるが、交換日記は毎日出したい。以下パッチを推奨:

`body.appendChild(buildDateMemoCard(key));` の呼び出し位置を Read で確認して、その**直下**に:
```javascript
    body.appendChild(renderNoteThread(key));
```

もし buildDateMemoCard が isBothOff 内でしか呼ばれてないなら、refreshDaySheet の後半(evs.forEach の後、anniv forEach の後)に:
```javascript
  body.appendChild(renderNoteThread(key));
```
を追加して、常に thread を表示する。

### Step 5.7: 未読バッジをカレンダーグリッドに追加

- [ ] **Locate render() 内のセル生成部 `if (key === todayKey) cell.classList.add('today');`**

Run:
```bash
grep -n "if \(key === todayKey\) cell.classList.add" "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **その**直後**に未読バッジ追加処理を挿入**

```javascript
    const unreadCount = (typeof computeUnreadForDay === 'function') ? computeUnreadForDay(key) : 0;
    if (unreadCount > 0) {
      const badge = document.createElement('div');
      badge.className = 'unread-badge';
      badge.textContent = unreadCount > 9 ? '9+' : String(unreadCount);
      cell.appendChild(badge);
    }
```

### Step 5.8: firestore.rules に notes 追記

- [ ] **既存の signals ルールブロックの下に notes を追加**

既存:
```
    // v11.0 signals/pulse - リアルタイム軽量信号
    match /signals/{docId} {
      allow read, write: if request.auth != null;
    }

  }
}
```

変更後:
```
    // v11.0 signals/pulse - リアルタイム軽量信号
    match /signals/{docId} {
      allow read, write: if request.auth != null;
    }

    // v11.0 notes - 交換日記スレッド
    match /notes/{dateKey} {
      allow read, write: if request.auth != null;
    }

  }
}
```

### Step 5.9: rules deploy

- [ ] **Run**

```bash
cd "C:\Users\tamah\Desktop\shift_calendar"
firebase deploy --only firestore:rules --project karenda-eba3e
```

Expected: `+  Deploy complete!`

### Step 5.10: ビルド + 検証 + コミット

- [ ] **ビルド**

```bash
python "C:\Users\tamah\Desktop\shift_calendar\build.py"
```

- [ ] **preview で検証**

```javascript
identityStore.set('kouki');
// セルタップして day-overlay 開く → "💬 交換日記" セクション表示
// 入力+送信 → メッセージ表示
// firestoreNotes を確認
Object.keys(firestoreNotes)

// 未読バッジ確認: 別 identity で書き込みしてから listenNotes 経由で反映
```

- [ ] **差分 + commit + push**

```bash
git add -A && git diff --cached --stat
git commit -m "$(cat <<'EOF'
v11.0-C5: 交換日記(notes)+ スレッドUI + 未読バッジ

- Storage.appendNote / markNoteRead / getDoc + arrayUnion 相当
- notes/{YYYY-MM-DD} スキーマ: messages[] 内に {id, from, text, ts, readBy}
- 現在月±1ヶ月のみ onSnapshot(コスト最適化)、shift() で listener 再構築
- renderNoteThread: day-overlay 内に thread + 入力欄
- computeUnreadForDay + カレンダーグリッドに未読バッジ(赤丸)
- Firestore rules に /notes/{dateKey} 追記 + deploy 済

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Self-Review

**Spec coverage:**
- ✅ Section 3 (favicon 4 state 優先順位) → Task 1 Step 1.1-1.2
- ✅ Section 4 (title 優先順位 + 更新頻度) → Task 1 Step 1.3-1.4
- ✅ Section 5 (時間帯背景 4 pattern + transition + reduce-motion) → Task 2 全 Step
- ✅ Section 6 (💗ボタン UI/フロー/identity/hidden タブ挙動/アニメ) → Task 3 identity + Task 4 全 Step
- ✅ Section 7 (notes スキーマ/UI/移行/未読バッジ/onSnapshot 最適化) → Task 5 全 Step(spec の"移行"は keep-both に変更、既存 memo card は buildDateMemoCard として温存)
- ✅ Section 8 (rules) → Task 3 Step 3.6-3.7 + Task 5 Step 5.8-5.9

**Placeholder scan:**
- TBD/TODO なし
- 各コード完結、Similar to Task N なし
- コマンドは具体、期待出力明示

**Type consistency:**
- identity: `'kouki' | 'yui'` 一貫、日本語 display は UI 内変換のみ
- Firestore keys: `signals/pulse`, `notes/{YYYY-MM-DD}` 一貫
- 関数名: identityStore.get/set, sendPulse, receivePulse, spawnHearts, handlePulseClick, appendNote, markNoteRead, renderNoteThread, computeUnreadForDay, applyTimeTheme, updateFavicon, updateTabTitle, updateAmbient, startAmbient
- CSS クラス: pulse-btn/pulse-heart/note-thread/note-msg/note-input-row/unread-badge/time-{morning,noon,evening,night}
- localStorage keys: `sc_my_person`

**Deviation from spec:**
- Spec Section 7 で "既存 memo → notes messages[0] に自動移行" と書いたが、複雑性回避のため **移行はせず、既存 memo card と交換日記 thread を共存** させる。既存 memo データは失わず、機能が二重になるだけ。

---

## 実装の進め方

1. **Task 1 (C1)** → build + preview で `document.title` `link.href` 動作確認 → commit + push
2. **Task 2 (C2)** → 時刻に応じた背景切替を preview で確認 → commit + push
3. **Task 3 (C3)** → 設定シートで identity 選択 → localStorage 保存 → rules deploy → commit + push
4. **Task 4 (C4)** → 💗ボタンタップ→ハート浮かび → 別端末で受信確認 → commit + push
5. **Task 5 (C5)** → セルタップ→交換日記 → メッセージ送信+バッジ確認 → rules deploy → commit + push

各コミット後に GitHub Pages 反映(1-2分)→ 実機(iOS/Android)確認。
